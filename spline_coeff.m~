function [c] = spline_coeff(x,f)
c = zeros(length(x), length();

for i = 1:length(x);
    n = length(f);
    for j = -3:-1

    end    
    for j = 0:n-4   
        [result] = evalBspline(j,3,f,x(i));   
        coef = coef + result;
    end
    for j = n-3:n

    end
end
disp(coef);
end